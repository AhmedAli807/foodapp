import 'package:flutter/material.dart';
import 'package:foodapp/Features/views/via_method/views/widgets/via_metod_view_body.dart';

class ViaMethodView extends StatelessWidget {
  const ViaMethodView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: ViaMethodViewBody(),
    );
  }
}
